[package]
name = "stt_realtime"
version = "0.1.0"
edition = "2024"

[dependencies]
windows = { version = "0.61", features = [
    "Media_SpeechRecognition",
    "Foundation",
    "Foundation_Collections",
    "Win32_System_Com",
] }
