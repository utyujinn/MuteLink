# MuteLink 作業計画書

## 方針概要

現在の Rust+eframe アプリを **Tauri v2** へ移行し、動作を完全に検証した後、
別バイナリとして **VRオーバーレイ** を追加する。

```
最終形態:
  mutelink.exe        ← Tauriデスクトップアプリ（ロジック全担当）
  mutelink-overlay.exe ← Rust/winit+wgpu+egui+OpenVR（薄いVRクライアント）
  ↑両者は localhost WebSocket (port 19998) で通信
```

---

## フェーズ1: Tauri移行

### ゴール
- 現在の全機能をTauriで再現する
- Chrome headless依存を完全に排除する
- 将来のVRオーバーレイ用IPC基盤を設計・実装する

---

### 1-1. 環境・プロジェクト構造セットアップ

**前提確認**
- Node.js v20 LTS インストール
- `cargo install tauri-cli`

**新しいディレクトリ構造**

```
MuteLink/
├── src-tauri/                ← Rustバックエンド
│   ├── src/
│   │   ├── main.rs           ← Tauriエントリポイント（新規）
│   │   ├── lib.rs            ← コマンド登録（新規）
│   │   ├── audio_device.rs   ← そのまま移行
│   │   ├── config.rs         ← パス変更のみ
│   │   ├── osc.rs            ← そのまま移行
│   │   ├── tts.rs            ← そのまま移行
│   │   ├── suffix.rs         ← そのまま移行
│   │   ├── speech.rs         ← 大幅簡略化（Chrome関連を全削除）
│   │   └── ipc.rs            ← オーバーレイ用WebSocketサーバー（新規）
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   └── build.rs
├── src/                      ← フロントエンド（Vanilla TypeScript）
│   ├── index.html
│   ├── style.css
│   ├── speech.ts             ← webkitSpeechRecognition ラッパー
│   ├── state.ts              ← アプリ状態・DOM更新
│   └── main.ts               ← エントリポイント・イベント配線
└── package.json
```

**Cargo.toml 変更**

| 変更 | 対象 |
|---|---|
| 削除 | `eframe`, `egui`, `tungstenite` |
| 追加 | `tauri`, `tokio` |
| 維持 | `windows`, `rodio`, `cpal`, `serde`, `serde_json`, `urlencoding`, `ureq` |

---

### 1-2. Rustバックエンドのコマンド化

既存モジュールを Tauri コマンドとして公開する。

**既存ファイルの移行方針**

| ファイル | 変更量 | 備考 |
|---|---|---|
| `audio_device.rs` | なし | そのままコマンドから呼ぶ |
| `osc.rs` | なし | そのままコマンドから呼ぶ |
| `suffix.rs` | なし | Config に同梱 |
| `tts.rs` | 小 | `speak()` を async 対応に |
| `config.rs` | 小 | ファイルパスを `app_data_dir()` へ変更 |
| `speech.rs` | 大 | Chrome 関連を全削除（後述） |

**実装するTauriコマンド**

```rust
// 認識制御
#[tauri::command] fn start_recognition(lang: String, device_id: Option<String>, state: ...) -> Result<()>
#[tauri::command] fn stop_recognition(state: ...) -> Result<()>
#[tauri::command] fn on_speech_result(app: AppHandle, r#type: String, text: String, state: ...)

// テキスト・送信
#[tauri::command] async fn confirm_text(text: String, suffix_idx: usize, state: ...) -> Result<()>
#[tauri::command] fn reset_chatbox(app: AppHandle, state: ...)

// デバイス・スピーカー
#[tauri::command] fn get_audio_devices() -> Vec<(String, String)>
#[tauri::command] async fn get_tts_speakers() -> Vec<VoiceSpeaker>

// 設定
#[tauri::command] fn load_config(app: AppHandle) -> AppConfig
#[tauri::command] fn save_config(app: AppHandle, config: AppConfig) -> Result<()>
#[tauri::command] fn find_voicevox_path() -> String
```

**Tauriイベント（Rust → フロントエンド）**

| イベント名 | ペイロード | タイミング |
|---|---|---|
| `speech-hypothesis` | `{text: string}` | 暫定認識テキスト更新 |
| `speech-final` | `{text: string}` | 認識確定 |
| `speech-state` | `{state: "idle" \| "capturing" \| "speech"}` | 状態変化 |
| `speech-error` | `{message: string}` | エラー発生 |
| `app-state` | `AppState` | VRCテキスト・pending変化時 |

---

### 1-3. 音声認識の移行（最重要・最大の変化）

**現在の構造（削除）**
```
Chrome headless
  ├─ HTTP サーバー (port 18888) ← Chrome が HTML を取得
  └─ webkitSpeechRecognition
       └─ WebSocket (port 19999) ──→ speech.rs → mpsc → UI
```

**移行後の構造**
```
WebView2（Tauriに内蔵、追加起動不要）
  └─ webkitSpeechRecognition（JS内で直接実行）
       └─ invoke('on_speech_result', {...}) ──→ Rust コマンド
```

**削除できるコード（speech.rs）**
- `launch_chrome()`, `find_browser()`
- `kill_process_tree()`
- `http_server()`, `ws_server()`
- `make_html()`, `HTML_TEMPLATE` 定数
- `ChromeHolder` 型と全プロセス管理コード
- `main.rs` の Chrome kill ブロック

**speech.rs の残存責務（Tauri State へ移行）**
- 認識状態管理（Idle/Capturing/SpeechDetected）
- デフォルトキャプチャデバイスの切り替えと復元

**フロントエンド側 (SpeechEngine.ts)**
```typescript
// WebView2 上で webkitSpeechRecognition を直接使用
declare var webkitSpeechRecognition: any;

export function startRecognition(lang: string) {
  const r = new webkitSpeechRecognition();
  r.continuous = true;
  r.interimResults = true;
  r.lang = lang;
  r.onresult = (e: any) => {
    const res = e.results[e.resultIndex];
    invoke('on_speech_result', {
      type: res.isFinal ? 'final' : 'interim',
      text: res[0].transcript
    });
  };
  r.onerror = (e: any) => {
    if (e.error !== 'aborted' && e.error !== 'no-speech') {
      invoke('on_speech_result', { type: 'error', text: e.error });
    }
  };
  r.onend = () => setTimeout(() => r.start(), 200);
  r.start();
}
```

---

### 1-4. フロントエンドUI作成

現在の egui UI を Svelte + CSS で再現する。

**ウィンドウ設定**
- サイズ: 440×560px（現状と同じ）
- リサイズ不可
- 常前面

**実装コンポーネント**

| コンポーネント | 主な実装内容 |
|---|---|
| `LanguageMicBar` | 言語/マイクのセレクトボックス |
| `StatusBar` | 状態ドット（緑/黄/グレー）＋エラー表示 |
| `HypothesisArea` | 暫定テキスト、青色、スクロール下部固定 |
| `PendingArea` | 編集可テキスト、ホバーで背景変化、Ctrl+Enter送信 |
| `VrcArea` | 紫テキスト、X/144カウンタ、リセット文字数到達で色変化 |
| `ButtonGrid` | 語尾ボタン×12 + 開始/停止/Auto/Chatbox/TTS（4列） |
| `SettingsMenu` | 結合モード、TTS設定、デバイス選択、VOICEVOXパス |
| `SuffixEditor` | モーダル、テーブル形式（7列） |

**キーボード操作（現状と同一）**
- `Ctrl+Enter`: 語尾0番（「。」）で送信
- `Ctrl+H/L/K/J`: カーソル左/右/上/下

**IME対応**
- `compositionstart` / `compositionend` でIMEコンポジション中のEnter送信をブロック

---

### 1-5. オーバーレイ用IPCサーバー

フェーズ2に向けて、Tauri バックエンドに WebSocket サーバーを追加する。

**仕様**
- ポート: `19998`（固定）
- プロトコル: JSON over WebSocket
- 方向: 双方向

**Rust→オーバーレイ（状態broadcast）**
```json
{
  "type": "state",
  "vrc_text": "今日はいい天気ですね。",
  "hypothesis": "明日も",
  "pending": "明日も晴れそう",
  "is_running": true,
  "auto_mode": false,
  "rec_state": "capturing",
  "vrc_will_reset": false
}
```

**オーバーレイ→Rust（コマンド）**
```json
{"type": "cmd", "action": "start"}
{"type": "cmd", "action": "stop"}
{"type": "cmd", "action": "confirm", "suffix_idx": 0}
{"type": "cmd", "action": "reset"}
```

**実装方針**
- アプリ起動時にバックグラウンドスレッドで起動
- 接続ゼロでも動作に影響しない（完全オプション）
- 状態変化のたびに全接続クライアントへ broadcast

---

### 1-6. 動作テストチェックリスト

**起動・終了**
- [ ] アプリが正常起動する
- [ ] 終了時に設定が保存される
- [ ] 終了時にVOICEVOXプロセスが停止する

**音声認識**
- [ ] 言語・マイクデバイスが一覧表示される
- [ ] 「開始」でWebView2上の認識が始まる
- [ ] Hypothesisエリアに暫定テキストが表示される
- [ ] 発話確定でPendingエリアにテキストが追加される
- [ ] 「停止」で認識が止まる
- [ ] 認識中のマイク切り替えで自動再起動する

**送信機能**
- [ ] 語尾ボタンを押すとTTSが再生される（VOICEVOX）
- [ ] OSCでVRChatのChatboxにテキストが送信される
- [ ] `Ctrl+Enter` で語尾「。」付き送信
- [ ] 送信後VRCエリアにテキストが表示される
- [ ] VRCエリアクリックでChatboxリセット送信

**モード**
- [ ] Autoモード: 発話確定で自動送信
- [ ] 手動モード: Pendingに蓄積→ボタンで送信
- [ ] 結合モード: テキストが追記される
- [ ] リセット文字数超過で橙色表示・次回送信でリセット

**TTS・デバイス**
- [ ] TTS出力デバイスを複数選択できる
- [ ] CABLE Input (VB-Audio) が自動選択される
- [ ] 感情（速さ/ピッチ/抑揚/音量）が語尾ごとに変わる

**設定**
- [ ] 設定が再起動後も維持される
- [ ] VOICEVOXパスの自動検出が機能する
- [ ] 語尾エディタでパラメータを変更できる
- [ ] 設定リセットが機能する

**IPC**
- [ ] port 19998 でWebSocketサーバーが起動している
- [ ] 状態変化時に正しいJSONが配信される
- [ ] コマンドを受信して処理できる
- [ ] クライアント切断後も動作継続する

---

## フェーズ2: VRオーバーレイ

（フェーズ1の全チェック通過後に着手）

---

### 2-1. プロジェクト追加

Cargo workspace に別バイナリとして追加する。

```toml
# MuteLink/Cargo.toml
[workspace]
members = ["src-tauri", "overlay"]
```

**overlay/Cargo.toml 主な依存**
```toml
[dependencies]
winit    = "0.30"
wgpu     = "22"
egui     = "0.29"
egui-wgpu = "0.29"
openvr   = "0.9"
tokio    = { version = "1", features = ["full"] }
tungstenite = "0.24"
serde    = { version = "1", features = ["derive"] }
serde_json = "1"
```

---

### 2-2. OpenVR Overlay基盤

- OpenVR 初期化・Overlay ハンドル作成
- wgpu Device/Queue の初期化（Vulkanバックエンド）
- egui を wgpu テクスチャへ描画するレンダーループ
- テクスチャを `SetOverlayTexture` で OpenVR へ毎フレーム送信
- コントローラーのLaser Pointer入力 → egui Pointer イベントへ変換

**参考実装:** `wlx-overlay-s`（Rust + wgpu + egui + OpenVR の構成が同一）

---

### 2-3. オーバーレイUI設計

デスクトップUIの簡略版。VRでの可読性・操作性を優先する。

**表示要素**
- 状態ドット + 認識状態テキスト
- Hypothesis テキスト（暫定）
- Pending テキスト
- VRCテキスト（文字数カウンタ付き）
- 語尾ボタン×12（コンパクト配置）
- 開始/停止ボタン

**入力**
- コントローラーのLaser Pointer でホバー・選択
- トリガーでボタン押下

---

### 2-4. Tauri IPC接続

```rust
// overlay/src/ipc.rs
// ws://127.0.0.1:19998 へクライアントとして接続
// 受信: AppState を反映してegui再描画
// 送信: ボタン操作をコマンドとして送信
// 再接続: 切断時に1.5秒後にリトライ（Tauriアプリより先に起動しても復帰）
```

---

### 2-5. VRオーバーレイ動作テストチェックリスト

- [ ] SteamVR起動中にオーバーレイが空間に表示される
- [ ] Tauriアプリより先にオーバーレイを起動しても接続待機できる
- [ ] Tauriアプリ起動後に自動接続される
- [ ] 音声認識テキストがオーバーレイに反映される
- [ ] コントローラーで語尾ボタンを押すと送信される
- [ ] Tauriデスクトップ画面と状態が同期している
- [ ] オーバーレイを閉じてもTauriアプリは動き続ける

---

## 技術選定まとめ

| 項目 | 採用 | 理由 |
|---|---|---|
| フロントエンド | Vanilla TypeScript | UIが単純なのでフレームワーク不要、依存が減る |
| Tauri バージョン | v2 | 安定版、WebView2対応 |
| IPC プロトコル | WebSocket + JSON | 既存知見あり、デバッグしやすい |
| VR API | OpenVR (openvr crate) | SteamVR対応、VRChat向けHMD全対応 |
| wgpu バックエンド | Vulkan (Windows) | OpenVRとの相性が良い |

---

## 作業順序・工数目安

```
フェーズ1
├─ [1-1] 環境・構造セットアップ         ～ 0.5日
├─ [1-2] Rustバックエンドのコマンド化   ～ 2日
├─ [1-3] 音声認識の移行                ～ 1日
├─ [1-4] フロントエンドUI作成           ～ 3～4日
├─ [1-5] IPCサーバー実装               ～ 0.5日
└─ [1-6] 動作テスト・修正              ～ 1～2日
                                    合計 ～ 8～10日

フェーズ2（フェーズ1全テスト通過後）
├─ [2-1] overlayプロジェクト追加       ～ 0.5日
├─ [2-2] OpenVR Overlay基盤           ～ 3～4日
├─ [2-3] オーバーレイUI               ～ 2日
├─ [2-4] IPC接続                      ～ 0.5日
└─ [2-5] VRテスト・修正               ～ 2日
                                    合計 ～ 8～10日
```
