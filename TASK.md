# Task

ＵＩの調整は最後にやります。今は機能を最小構成で作るというところにフォーカスしてほしい。

- 言語切り替え
堅固を日本語、英語、中国語で切り替えれるように。メイン画面に配置。

- VRCのチャットボックスに文字を送信する機能（トグル可能）

- 確認してから音声を送信する機能
画面のブロックとして、一番上に認識中の文字、その下のブロックに認識が終わったFinalの文字、その下に実際にVRCへと送信した後の文字を配置する。文字の送信モードは、Autoモードと手動モードで切り替えることができて、Autoの時は認識したFinalの文字をそのまま送信。手動モードの時は自分で確認してから送信する。

- 語尾を追加
文字を確認してから送信するモードの時に、自分が好きな文字から選べるようにする。その際に、VoiceVox処理をすると思うんだけど、声のトーンとかいろいろパラメータがあったと思うからそこを自分で調整できるようにする。

- ホットキー機能
VRのコントローラをホットキーとする。裏の中指ボタンと人差し指のボタン（Quest）想定で語尾の確定、送信をできるようにする。もしも認識が誤っていたときはそこで消せるようにもする。具体的には、二つとも押した状態、片方押した状態（２パターン）の３つの語尾をホットキーとして設定できるようにする。これらを押して１ｓたったら文字を確定して送信する。もしＦｉｎａｌになってからどちらも押されない状態が３ｓ続いたらその文字は破棄する。

- メイン画面
  チャットボックスにOSCで送信するか否かのトグル
  音声出力をするか否かのトグル
  自動送信モードと確認して語尾を指定してから送信モードの切り替え

OK,順番通りに行ったら次はメイン画面だけど、設定画面を先に作ってほしい。CSSを整えてから始めよう。詳細を固めてからやっていこう。まず、設定画面のレイアウトなんだけど、ウィンドウ全体の左側２０％に大項目を並べて選択できるようにして、

General画面で語尾の設定（１２個分（固定）の語尾とそれらの速度とかのパラメータを横長の棒を横に動かす形で調整、それぞれの語尾に対して開く閉じるを行うことができて、開く前は語尾と数値のみ記載、開いた後にさっき書いた棒に丸が刺さっているものを数行つけて調整できるように）、出力音声のキャラクター設定（まだ中身は実装しなくてよい、区画だけ置いておく）、設定全リセットボタン（確認のプロンプト付き）

マイク、スピーカーについて、Device画面で設定を記載する。それぞれで自動選択か手動選択か切り替えるトグルスイッチと、マイクはラジオボタンで単一選択できるラジオボタン、スピーカーは複数選択できるラジオボタンを置いてほしい。文字が横に長くなりすぎることがあると思うので、20文字を超えたらそれより横はかくしてscrollをループして全体が見えるようなレイアウトにしてほしい。

AppearenceではUIサイズとフォントサイズをそれぞれ棒の上を左右に動かす丸で調整できるようにしたい。フォント変更はドロップダウンメニューで。ダークモード、ライトモード、システムのdefaultを使用というのもラジオボタンで切り替えれるようにしたい。

Otherはとりあえず何もなしで


- 設定画面の作成
  - Device
    マイクとかスピーカー出力とかを設定
  - Appearence
    UIのサイズ、フォントサイズ、フォント変更
  - Other
