# CLAUDE.md

このファイルはClaude Codeがこのプロジェクトで作業する際の入口です。実装作業を始める前に、
必ず以下のドキュメントを(まだ読んでいなければ)読んでください。

- [TASK.md](TASK.md) — タスクリスト。番号付きの未完了項目を上から一つずつ、ユーザーと確認しながら進める。
  完了した項目はチェック済みにする(取り消し線または `[x]` 等、既存の記法に合わせる)。
- [README.md](README.md) — プロジェクト概要・利用者向けセットアップ・開発者向けビルド手順。
  「現在の実装」節はアーキテクチャの現状を反映させておくこと(機能追加時に更新を忘れがち)。
- [docs/CODING_RULES.md](docs/CODING_RULES.md) — ディレクトリ構成・コーディング規約。
- [docs/GIT_WORKFLOW.md](docs/GIT_WORKFLOW.md) — コミット運用。
- [docs/TESTING.md](docs/TESTING.md) — 自動テストが無いプロジェクトでの動作確認の仕方。

## プロジェクトの性質

- Windows専用のTauri v2デスクトップアプリ。開発者(GitHub: `utyujinn`)一人のみのソロ開発。
- ユーザーはVRChatで自分の声を出さずにコミュニケーションしたい。音声認識→(必要なら手動編集)→
  VOICEVOXでの読み上げ(VB-Audio Virtual Cable経由でVRChatのマイクへ)、および/またはOSCでの
  チャットボックス直接送信、の両方に対応する。VR内オーバーレイHUD+コントローラーのホットキーと、
  VRを使わないデスクトップ操作(TASK.md 12番)の両方をサポートする。
- **自動テストが存在しない**プロジェクト。CIも「ビルドしてインストーラを作る」ところまでで、
  ユニットテストの実行は含まない。実装が正しいかどうかは最終的に実際にアプリを動かして確認する
  ([docs/TESTING.md](docs/TESTING.md) 参照)。
- **単一エージェント運用**。uniahub等の他プロジェクトで使っているような役割分担(実装係/レビュー係/
  デザイン係に分けたマルチエージェント運用)はこのプロジェクトの規模には合わないため採用していない。
  複数観点からの調査や大規模な原因調査(例: IPC応答が返ってこない不具合の原因特定)には`Agent`
  ツール(`subagent_type: Explore` や `general-purpose`)を随時使ってよいが、実装・コミット判断は
  メインセッションが直接行う。

## 作業の進め方

1. [TASK.md](TASK.md) を上から確認し、未完了の最初の項目に着手する。項目が大きい場合は、
   ユーザーに何をどこまでやるか一度確認してから着手する。
2. 実装したら、ビルド([docs/TESTING.md](docs/TESTING.md) の該当する確認方法)で壊れていないことを
   確認する。UI/挙動が絡む変更は、変更内容を説明した上でユーザー自身に実際のアプリで確認してもらう
   (自動テストが無いため、これが実質的な受け入れ確認になる)。
3. 設計判断の理由(なぜその実装を選んだか)は、コード内のコメントとして残す
   (「何をしているか」ではなく「なぜそうしているか」を書く。[docs/CODING_RULES.md](docs/CODING_RULES.md)参照)。
4. UIの文言を追加・変更した場合は、4言語(ja/en/zh/ko)すべてのI18Nエントリを更新する
   ([docs/CODING_RULES.md](docs/CODING_RULES.md) のI18N節参照)。
5. コミットは [docs/GIT_WORKFLOW.md](docs/GIT_WORKFLOW.md) に従う。`git commit`/`git push` は
   ユーザーから明示的に依頼された時のみ行う(無断でコミットしない)。

## やってはいけないこと

- `git push --force` / `git reset --hard` などの破壊的操作は、ユーザーに確認してから行う。
- コード署名鍵・GitHub Actionsのシークレット等を、コードやドキュメントにハードコードしない。
- バンドルするVOICEVOXキャラクターやSenseVoice/Whisperモデルなど、外部モデルの利用規約を
  確認せずに追加しない(既存はTASK.md 6番で確認済み)。
- 「起きないはずのケース」への防御的なtry/catchやフォールバックを書かない。内部ロジックのバグは
  そのまま失敗させ、原因を直す。
